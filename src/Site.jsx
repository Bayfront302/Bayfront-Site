export default function Site() {
  // Keep this ultra-basic to verify render; swap in your real UI once you see it.
  return (
    <main style={{ padding: 24, fontFamily: "system-ui, sans-serif" }}>
      <h1>It works! 🎉</h1>
      <p>
        If you see this on Netlify, React is rendering correctly.
        Next, replace this component with your real content.
      </p>
    </main>
  );
}
